export interface ScrollProgressProps {
  scrollYProgress: any; // Framer Motion MotionValue
}

export interface WatchPartProps {
  scale: any;
  rotate: any;
  y: any;
  opacity: any;
}

export enum SectionTheme {
  DARK = 'dark',
  LIGHT = 'light',
  GOLD = 'gold'
}